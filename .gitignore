# Dependencies
node_modules/

# Environment (secrets — never commit)
.env

# OS artifacts
.DS_Store

# Legacy local DB (from an older SQLite version of this app)
wastewatch.db*

# Local uploads / build artifacts
public/uploads/
.dist/
